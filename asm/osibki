✅ невалидная строка: ld %0, r2,

много/мало/не подходящие/нет вообще аргументы для операций

✅ до/между/после .name/.coment и "" не должно быть никаких символов и после тоже

✅ бесконечный цикл если в имени/комменте нет последних/первых ковычек
проверять лэйблы на правильные чары

✅ сигфолт:
live:
        live %0
        ld r2
        zjmp %:loop
:

✅ сигфолт:
live:
        live %0
        ld r2
        zjmp %:loop
,


✅ сигфолт:
loop:
live:
        sti r1, %:live, %1
        live %0
        ld r2
        zjmp %:loop

✅ сигфолт если между операцией и аргументом нет пробела

✅ несуществующая метка

✅ не должно быть валидно %-0-0

✅ максимальный регистр должен быть 99

✅ несуществующая оерация

две операции в одной строке

две метки в одной строке не должно быть валидно